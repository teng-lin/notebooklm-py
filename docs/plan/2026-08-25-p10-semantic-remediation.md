# Semantic-boundary remediation plan (programme P10)

**Revision:** 3.1 (2026-08-25) — **converged at 2.8; repackaged from 31 PRs into 7 at the owner's direction** (the 31 slices are unchanged and become ordered commits inside 7 workstream PRs; see §3a). Reviewers: three native Claude lenses (A boundary/correctness, B compatibility/ADR/tests, C sequencing/cost), Codex CLI, agy CLI. Round 1 (rev 1): all five REVISE — 12 + 7 + 6 + 10 + 3 blocking. Round 2 (rev 2.1): agy APPROVE; A/B/C REVISE with every round-1 item resolved and 4 + 3 + 3 new local blockers; Codex REVISE with 6. Round 3 (rev 2.3): A, B, C, agy APPROVE (non-blocking nits only, folded in 2.4–2.6); Codex REVISE with 7 (three already fixed, four new, folded in 2.7). Bounded delta check (rev 2.7): Codex APPROVE, 7/7 resolved. Final tally: 5/5 APPROVE; two bookkeeping counts fixed in this revision. Every blocking item is addressed below; the change log is in §9.
**Target:** `refactor/semantic-backend-dev` @ `f79b5008` (PR #2261). All PRs are follow-ons after #2261 merges (it is `CLEAN` with 347 commits; nothing here is small enough to fold in without reopening bot review).
**Repo:** `/Users/blackmyth/src/notebooklm-py/.worktrees/semantic-backend-dev` (baseline `main` at `/Users/blackmyth/src/notebooklm-py`).
**Inputs:** defect list S1–S9, N1–N4 (three-reviewer consensus), ADR-0035 and its plan `docs/plan/2026-08-13-semantic-backend-refactor.md` ("the plan"), the P9.2 gate table `docs/plan/2026-08-24-p9-composite-gate-table.md`.
**Scope rule:** internal architecture only. No public import, signature, return type, exception class/field (`RPCError.method_id`, `rpc_code`, `raw_response`, the permanent `rpc_id`/`code` aliases — `docs/stability.md:334-336`), adapter payload, telemetry, profile format, or cassette change. Every PR is independently green under the required CI set (`uv run pytest -n auto --dist loadgroup -m "not repo_lint and not requires_playwright and not requires_chromium" --no-cov`), plus `mypy`, `ruff`, and the `repo_lint` guardrails it touches.

**Phase token.** This programme is **P10** for the purposes of `tests/_guardrails/test_no_anonymous_bridges.py` (`CURRENT_PHASE` is bumped 9 → 10 in R0.1). Any transitional module or branch must carry `Removal: P10` and be deleted within this programme; there are no "one-release" shims (migration rule 1, plan `:727-731`).

---

## 0. Defects in scope (short form)

| ID | Defect | Sev |
|---|---|---|
| S1 | Source-add workflows run below the port on public models; four rows `RAW_PASSTHROUGH` leak public exceptions through `SourceService` | High |
| S2 | Mind-map rows adapt `RowInvoker` back to `RpcCaller` (`_web/bindings/_invoker_caller.py`) to run `LegacyNoteBackedService`; neutral `NoteService` shares the module | High |
| S3 | Chat: codec builds mutable public `ChatReference` → record → public; positional request grammar in `_chat/stream_request.py` imported by `_web/codec`; private-import shims; `CHAT_ASK` custom row with head-injected collaborators because only batchexecute is a native kind | High |
| S4 | Studio generation: source-id/language defaulting and option validation below the port; 11 "deferred-product" rows | Medium |
| S5 | Chat workflow (locks, cache, session hint, follow-up) in the 830-line facade; `ChatService` is a pass-through (deliberate P6.1 deferral, plan `:1556-1560`) | High |
| S6 | `ArtifactsAPI` deprecated private constructor builds a partial `WebRpcBackend`; `get_prompt()` runtime legacy branch (dead: no test reaches `_artifacts.py:381`) | Medium |
| S7 | `ResearchService` projects public exceptions itself and branches on public exception types; dead `project_source_add_error` | Medium |
| S8 | `_web/registry.py` repeats 80 `OperationDef`s; `_web/policy.py` hand-copies natives (79/80 derivable; `chat.ask` divergence records a native the row does not dispatch) | Low |
| S9 | Nine primitives indistinguishable from product operations; 27 flat root-level modules | Low |
| N1 | Only `_label_service`, `_source_service` return records; 13 service modules import `_projectors`/public types/`httpx`; `_sharing_service` mixes `ShareStatus` and `ShareStatusRecord` | Med–High |
| N2 | Capabilities cannot distinguish "not a product feature" from "available through a service workflow" (`research.wait`/`import_verify` implemented in `ResearchService`, registry says unsupported) | Medium |
| N3 | Two deadline-expiry paths in `WebRpcBackend.invoke` (`backend.py:365-371`) | Low |
| N4 | `WebRpcBackend.__init__` has 15 parameters; `chat_reqid` duplicates `reqid` (`_client_composition.py:256-266`; `self._reqid` is write-only today, `backend.py:143`, and becomes the transport's counter in R2.2) | — |

Out of scope: the record→public-model projection layer and `_backend_compat.py`'s legacy-exception reconstruction (compatibility tax owed to the public contract; retire only on an ADR-0018 runway). A mobile backend. The byte-download clients in `_studio/downloads.py` (`httpx` + `_curl_cffi_transport`) — listed as a permanent, named I1 exemption pending a separate download-transport slice.

---

## 1. Governance decisions and addenda (owner sign-off before R0)

The round-1 reviews found four places where revision 1 silently contradicted decisions ADR-0035 or the plan had frozen. Each is now an explicit decision with an ADR-0035 addendum deliverable. All addenda land in **R0.0**, one docs-only PR, so every later PR executes under amended rules.

| ID | Decision | Options | Recommendation |
|---|---|---|---|
| D1 | Studio generation row shape (S4) | (a) required, pre-resolved input records on the existing generation defs; rows become single-native `CodecBinding`s; "None = all sources" becomes a documented service-level default. (b) per-family `PRIMITIVE` leaves (8 `artifact.generate_*` + `mind_map.generate_*` ×2 + `suggest_prompts` = up to 11 new members) with the product defs service-owned, preserving the optional-input contract at the port. (c) one generic family-keyed kickoff leaf — **rejected**: violates "OperationDef binds one concrete input type" (`_web/backend.py:381-385` exact `type(value) is input_type`) and cannot cover `SUGGEST_PROMPTS`/`GENERATE_MIND_MAP`. | **(a)**, recorded as an ADR-0035 addendum that source-scope defaulting is a service concern (agy, Claude). Codex and the plan (`:1973-1988`) prefer (b); the owner decides. Cost delta: (b) adds ~11 enum members and pins. |
| D2 | Extend the ADR-0035 value-type exemption to the frozen `ConversationTurnKey` / `NextStepSuggestion`? | yes / **no** | **no** (Codex, adopted). Neutral `ChatTurnKeyRecord` and `ChatNextStepRecord` already exist (`_chat_records.py:51`) with projectors (`_projectors.py:470`), and `_types/chat.py` imports `rpc.types` (`:9`), which disqualifies it from the closed-stdlib exemption (ADR-0035 `:75`). The codec emits the existing records; no exemption change. |
| D3 | Policy-ledger independence (S8; ADR-0035 `:215`, ADR-0005 `:69`, plan `:2082`) | derive everything (rejected: the audit would compare rows/registry with themselves — Codex, lens-compat) / status quo / **reviewed intent stays hand-written but moves out of production** | **third option.** The *actual* native set is derived from `NativeCallSpec.choices` (`RpcNative` only). The *expected* natives, roles, per-native expected policy, and `known_divergence` remain hand-reviewed — independent of both rows and registry — but live outside production in `scripts/_web_policy_intent.py` (beside `scripts/_operation_catalog_specs.py`, the existing hand-reviewed catalog metadata home; `scripts/` never imports `tests/`) plus the stored baseline `web_policy_parity.json` under `tests/fixtures/baselines`. Production `_web/policy.py` is deleted; `CallPolicy` stays on each `OperationDef`. The audit compares actual↔expected natives and expected↔registry policy, and distinguishes direct-row parity from end-to-end operation authority (the `chat.ask` `GET_NOTEBOOK` entry is an intentional end-to-end recency divergence, gate table `:576`, and stays recorded). Runs as a test, not at registry construction (`registry.py:113,347-350`). ADR-0035 addendum amends the ADR-0005 row accordingly. |
| D4 | `source.add_file` stays a `protocol` custom row (Scotty choreography) | yes | **yes**; its result/failure graph becomes neutral (R3.1). |
| D5 | **ADR-0035 principle-2 addendum: retire the protocol-variance classification of source registration** (`SOURCE_ADD_URL`, `_URL_BATCH`, `_DRIVE`, `_TEXT`; gate table `:370-371,383-395`; plan `:1838-1839,:2377`) and authorise the `SOURCE_REGISTER` primitive leaf | addendum / keep adapter-owned | **addendum**. The mobile "tentative source" variant is hypothetical; the cost of keeping four workflows on public models below the port is real (S1). |
| D6 | **ADR-0035 principle-5 addendum: capabilities views** | change `supports()` to direct ∪ service-owned (rejected: breaks `invoke`'s gate `backend.py:340-342`, `RecordingBackend` `:167`, `require_leaves` `_backend.py:311-322` and its 8 call sites, `test_backend_foundation.py:85-86`) / **keep `supports()` = directly invokable; add `workflows: frozenset[Operation]` and `available(op) = supports(op) or op in workflows`; lift the plan's rule-4 deferral of `supports_workflow` (`:2005-2016`)** | **second option**. N2 is fixed by `available()`; `require_leaves` keeps gating on `supports()`. No production consumer reads `capabilities` today except `require_leaves`/`invoke`; `workflows` is a data field asserted by `test_semantic_capabilities.py` and reported by `scripts/audit_operation_catalog.py`'s capability column — no product code branches on it. |
| D7 | **ADR-0035 addendum for I1**: the ADR's Decision lists "public model projectors" among permitted service dependencies; I1 forbids them so that projection is a facade responsibility | addendum / drop I1 | **addendum**. Without it R0.1 would enforce a rule the ADR permits. |
| D8 | Programme phase token P10 | — | bump `CURRENT_PHASE` (`test_no_anonymous_bridges.py:12,34,43`; no `Removal: P9` marker exists in `src/` today, so nothing turns red) in R0.1. |
| D9 | **ADR-0035 principle-2 addendum for `chat.ask`**: the plan names chat as protocol-varying (`:1825`) and adapter-owned; R2.2 makes the product operation service-owned over a `CHAT_STREAM_ANSWER` primitive | addendum / keep adapter-owned | **addendum** (same reasoning as D5: the variance is hypothetical, the collaborator leak is real). |

---

## 2. Target invariants (guardrails; definition of "fixed")

| # | Invariant | Enforcement and seed |
|---|---|---|
| I1 | A semantic service module (`_*_service.py`, `_read_services.py`, `_source_add_reports.py` (R3.4), `_chat/service.py` (→ `_chat/workflow.py (new)`), `_studio/*.py`) imports none of `_projectors`, `notebooklm.types`, `_types.*`, `_backend_compat`, `rpc.*`, `_row_adapters.*`, `_web.*`, `httpx`; public methods return `*Record`/`*Result`, neutral enums, built-in scalars/collections thereof (e.g. `get_source_ids() -> list[str]`, `_read_services.py:55`), or `None`. | `tests/_guardrails/test_service_boundary.py` (**new**; subsumes `test_semantic_read_boundary.py:7-8`, which is retired in the same PR under rule 9). Seed allowlist = **13 modules**: the 8 root services (`_read_services`, `_notebook_mutation_service`, `_research_service`, `_settings_service`, `_sharing_service`, `_suggestion_service`, `_notebook_guide_service`, `_note_service`) + `_studio/catalog.py:9,20`, `_studio/lifecycle.py:17`, `_studio/mind_maps.py:10,23`, `_studio/representations.py:20`, `_studio/downloads.py:22` (permanent named exemption, see scope). Ratchet: shrinks only; hard rule when only the exemption remains. |
| I2 | `_web/**` imports no domain package (`_chat`, `_source`, `_studio`, `_artifact`, `_mind_map`, `_note_service`, `_label_service`, …). Neutral helper modules (`_records*`, `_research_neutral`, `_deadline`, `_request_types`) are permitted. | Same file. Seed = **10 files**: `codec/chat_stream.py:19,22`, `codec/chat.py:10`, `backend.py:64,69`, `bindings/mind_maps.py:30-31`, `bindings/sources.py:83-89`, `codec/generation.py:18`, `codec/mind_maps.py:10`, `codec/studio_documents.py:8` (all `_artifact.payloads`), `codec/artifacts.py:9` (`_artifact.formatters`), `codec/sources.py:568` (`_source.markdown`). Every entry has a retiring PR (§3). |
| I3 | Every `CustomBinding` row is `protocol` or `compatibility`; `deferred-product` count 0 and the category is deleted. | Existing ratchet `test_web_binding_ratchets.py:62` (`CUSTOM_ROW_COUNTS`). |
| I4 | `ROW_COLLABORATOR_NAMES == {"source_uploader"}`. | Pin in `test_web_backend.py`. Today: 6 (`source_uploader`, `deadline_factory` — declared by no row, removable in R0; `capture_public_failure` — goes with R3.2; `chat_reqid`, `chat_timeout`, `chat_transport_composed` — go with R2.2). |
| I5 | `BackendCapabilities.available(op)` is true for every operation the client performs through this backend; `supports(op)` remains "directly invokable". | `tests/unit/test_semantic_capabilities.py` (**new**). |
| I6 | `OperationDef.tier ∈ {PRODUCT, PRIMITIVE}`; facades never invoke a `PRIMITIVE` def; product counts exclude primitives. | Guardrail (**new**) + catalog `tier` field (ADR-0022 regen). |
| I7 | Actual native sets are derived from `NativeCallSpec.choices`; reviewed expected natives/roles/policies/divergences live outside production (`scripts/_web_policy_intent.py` + `tests/fixtures/baselines/web_policy_parity.json`, registered in `tests/_baselines/registry.py` and `docs/development.md:932-943`); no production module hand-lists natives; the audit runs as a test. | R2.5. |
| I8 | No `ErrorMode.RAW_PASSTHROUGH` rows; the enum member is deleted. | Pin. |
| I9 | No `Legacy*` class below `_app` except the enumerated exemptions: legacy-mapping records used only by `_backend_compat`/projectors (`_sharing_records.py:178,187`, `_chat_records.py:17,24`), auth storage migration (`_cookie_persistence.py:114`, `_auth/profile_migration.py:61,98,110,226,327`). Deletion targets: `LegacyNoteBackedService` (`_note_service.py:94`), `NotebookLegacyRpc` (`_notebooks.py:37`). | Guardrail (**new**) with the enumerated exemption set. |
| I10 | New records go in the domain `_*_records.py` modules, never `_records.py` (exactly 1,500 lines; `test_module_size_ratchet.py:240` fails at > 1,500). | Existing ratchet. |

---


## 3a. PR packaging — 7 PRs

The owner set a ceiling of 7 PRs. The 31 slices below (R0.0 … R7.3) are **unchanged in content, order and acceptance criteria**; they are packaged as ordered commits inside seven workstream PRs. Migration rule 2 is preserved at the *slice* level — every commit leaves one execution authority per operation **and carries its own pin deltas and catalog regeneration** (several pins are required-CI: `test_operation_catalog.py:39`, `test_binding_core.py:112`, `registry.py:318-319`, the signature/`vars` pins, `test_module_size_ratchet.py`), so every commit is green and the PR can be bisected — and at the PR level, since each PR ends with all of its operations owned once. Rollback is per PR (revert) and **linear**: reverting PRn after PRn+1… have merged requires reverting those first (PR3–PR5 build on PR2's derived registry and intent module). The slice commits are the bisection ladder. This repository already reviews at this scale (#2261: 557 files, 347 commits).

| PR | Contents (ordered commits) | Days | Pins & baselines touched (each in its slice commit; refreshed again at rebase when the queue advances) |
|---|---|---|---|
| **PR1 Foundations** | R0.0 addenda · R0.1 guardrails + P10 · R0.2 tier + capability views · R0.3 deadline path · R1.1 `ArtifactsAPI` constructor · R1.2 dead code · R1.3 payload/formatter moves | 8 | CAT (tier field, reasons), guardrail seeds, `test_semantic_read_boundary.py` retired, I4 pin (`deadline_factory` dropped), `test_backend_foundation.py`, signature/`vars` pins + the 13 test files constructing `WebRpcBackend(transport_factory=)`, `_client_composition.py`, WPI/RBI, ARCH |
| **PR2 Chat + tables** | R2.1 codec owns wire, records · R2.2 `StreamNative` + `CHAT_STREAM_ANSWER` + service-owned `CHAT_ASK` · R2.3 `ChatWorkflowService` · R2.4 I2 for chat · R2.5 derive registry, delete `_web/policy.py`, `scripts/_web_policy_intent.py` + `web_policy_parity.json` | 12 | OPS +1, SO +1, CRC protocol −1, KASO, CVI, WPI, RBI, CAT, OBS, I4, P8 inventory, constructor-signature/`vars` pins, `test_no_rpc_method_below_port.py:86`, the 21 `_web.policy` importers, baseline registry, `docs/development.md`, ARCH |
| **PR3 Source-add** | R3.1 neutral failure graph + upload port + `UploadLifecycleHooks` · R3.2–R3.5 hoist `add_text`, `add_url`, `add_drive`, `add_url_batch` (one commit each) | 9 | OPS +1, SUP −3, SO +4, CRC compat −1 / protocol −3, I8, KASO, WPI (`backend.py:69` retired), RBI, CAT, `ROW_COLLABORATOR_NAMES` |
| **PR4 Notes + mind-maps** | R4.1 legacy split · R4.2 workflows above the port, `MIND_MAP_GENERATE`, scoped `httpx` net, `NotesAPI` migration · R6.6 `NoteService` records | 8 | OPS +1, SUP −2, SO +3, CRC compat −3, I9, KASO, WPI, RBI, CAT, ARCH |
| **PR5 Studio** | R5.1a/b/c resolve inputs above the port (per D1) · R5.2 `_studio` I1 | 7 | CRC deferred-product −11 (category deleted), SUP/SO per D1, KASO, CVI, WPI, RBI, CAT, cassette replay |
| **PR6 Service return types** | R6.1 read services · R6.2 notebook services + `get_raw` via `NOTEBOOK_GET` + `NotebookLegacyRpc` deleted · R6.3 sharing/settings · R6.4 research neutral contracts + typed `research.wait`/`import_verify` · R6.5 purge (I1 → exemption only, hard rule) | 8 | SO +2 (+1 if `NOTEBOOK_METADATA_DEF`), CAT, I1 hard rule, I9 pin (`NotebookLegacyRpc`), `__cause__` characterizations |
| **PR7 Namespace** | R7.1 records/projectors/compat · R7.2 services · R7.3 port → `_semantic/` (no shims) | 6 | CAT path-only regen, `test_module_size_ratchet` keys, ARCH (`check_docs_module_refs`) |

**Order and parallelism.** PR1 first. PR2 next (it derives the registry and deletes the ledger every later row change would otherwise hand-edit). PR3, PR4, PR5 are developed as **stacked branches** (PR4 on PR3's head, PR5 on PR4's — they share `_web/registry.py`, the count pins, and PR4/PR5 both rewrite `_web/bindings/mind_maps.py`, `_web/codec/mind_maps.py`, `_studio/mind_maps.py`) and merged in that order; developing them against PR2's head instead would cost a re-implementation at rebase. PR6 after PR2–PR5 (its purge gate needs the chat, source, notes and studio services to conform). PR7 last. Total ≈59 engineer-days (the increase over rev 2.8 is rebase cost of the larger, stacked merges). Schedule effect of the grouping: R6.1–R6.4 lose their early-start parallelism and five I1 allowlist entries persist through PR2–PR5 — not a rule breach.

**What the larger PRs cost.** Revert granularity is per workstream, not per operation; bisection is by commit. Bot review (`gemini-code-assist`, `claude[bot]`) comments once per PR on ~10× the diff of a slice, so each PR body must carry the per-slice table (slice · operations touched · pins delta · tests moved/deleted with `pytest --co` counts) that the 31-PR form put in separate PR bodies.

## 3. Workstreams and slices (the commits inside the 7 PRs)

Each slice: one execution authority per operation after its commit (migration rule 2), no runtime old/new fallback, bisectable; rollback = revert the containing PR. Each slice lists the **pins & baselines it touches** (the repo's per-slice ritual, plan `:2352-2360`). Abbreviations: OPS = `test_operation_catalog.py:39` (`== 96`); SUP = `registry.py:318` / `test_binding_core.py:112` (80); SO = `registry.py:319` (11); CRC = `CUSTOM_ROW_COUNTS {5,4,11}` (`test_web_binding_ratchets.py:62`); KASO = `KNOWN_ACTIVE_SEMANTIC_OPERATIONS == 80` (`test_semantic_p7_entry_audit.py:42,835`); WPI = `KNOWN_WEB_PACKAGE_FIRST_PARTY_IMPORTS` (`test_semantic_p8_provider_boundary_audit.py:81`, exact set); CVI = `_REVIEWED_CODEC_VALUE_IMPORTS` (`test_p3_codec_model_boundary.py:31,38-45`); RBI = `REVIEWED_BACKEND_IMPORTS`/`ACTIVE_BACKEND_INVOKE_SITES` (`scripts/_operation_catalog_ast.py:901,1919`); CAT = `operation_catalog.json` regen (+ `scripts/_operation_catalog_specs.py` where dispositions change); OBS = `semantic_p7_observability.json`; ARCH = `docs/architecture.md` per-file index (`scripts/check_docs_module_refs.py`).

### R0 — Governance, invariants, cheap correctness (≈3 d)

**R0.0 Addenda (docs only).** ADR-0035 addendum covering D1(a or b), D2, D3, D5, D6, D7; `docs/plan/` entry for P10 with this plan. Touches: none.

**R0.1 Guardrails and phase token (I1, I2, I9, I10; D8).** New `test_service_boundary.py` with the three seed allowlists above; retire `test_semantic_read_boundary.py`; bump `CURRENT_PHASE` to 10; drop the `deadline_factory` collaborator (declared by no row). Touches: none beyond the new/retired guardrails. Acceptance: seeds match the live inventory exactly; ratchets fail on any growth.

**R0.2 `OperationTier` and capability views (I5, I6; N2; D6).** `OperationTier` on `OperationDef` (default `PRODUCT`; the nine P9.2 primitives marked `PRIMITIVE`); `BackendCapabilities.workflows` + `available()`; `WebRpcBackend` passes `workflows=WEB_SERVICE_OWNED_OPERATIONS`; `RecordingBackend` mirrors it; `require_leaves` keeps `supports()`. The five "unsupported" operations get explicit reasons; `research.wait` and `research.import_verify` have enum members but **no `OperationDef`** (`_operations.py:95`; unsupported registry entries carry `definition=None`, `registry.py:368`), so they stay `UNSUPPORTED` here with the reason "facade/service workflow without a typed def" and are flipped to `SERVICE_OWNED` in R6.4 once typed inputs/results, leaves and intent edges exist; `notebook.metadata` → `UNSUPPORTED` with the reason "raw `GET_NOTEBOOK` through `NotebookLegacyRpc`, `_notebooks.py:501`; migrates in R6.2"; `label.sources`, `collection.notebooks` → `UNSUPPORTED` with a reason. Guardrail: facades never reference a `PRIMITIVE` def. Touches: CAT (tier field + reasons), `test_backend_foundation.py`. Acceptance: `available(NOTEBOOK_CREATE)` true and `supports(NOTEBOOK_CREATE)` false (service-owned); product count 87. (`available(RESEARCH_WAIT)` becomes true in R6.4.)

**R0.3 Deadline path unification (N3).** One pre-dispatch expiry check in `invoke_binding` **after** encode/selector resolution (keyed specs pick the native from the input, `_binding.py:115-121`), honouring `DeadlineMode.IGNORE` (`_binding.py:611`; `source.wait` is the only IGNORE row), and routed through `_tag_native`/`map_error` (`_binding.py:622-628`) so `binding_native` assertions hold (`test_binding_core.py`, `test_web_binding_rows_{sources,notebooks,studio}_custom.py`, `test_web_binding_rows_chat_ask.py`). Remove the `not isinstance(binding.row, CodecBinding)` branch from `WebRpcBackend.invoke`. Acceptance: one path; `method_id` present for codec and keyed rows; stream rows carry none (stated, not implied); tests for expired `SOURCE_WAIT` (IGNORE), a keyed codec row, a custom row, and exact `method_id`.

### R1 — Quick wins (≈5 d)

**R1.1 `ArtifactsAPI` single constructor (S6).** Delete the `rpc` and `note_service` constructor inputs, `_legacy_constructor`, the dummy-transport `WebRpcBackend`, the `get_prompt()` legacy branch (`_artifacts.py:369-386`, dead), and the raw-row branches in the private parse helpers (`:1250-1317`, characterization moves to `_web/codec/artifacts` tests). **`mind_maps` stays until R4.2** (`_list_mind_maps` `:1216-1219` uses it outside the legacy branch; `NotesAPI` shares the graph). 68 `ArtifactsAPI(` sites in 24 test files migrate to `build_web_backend`/`RecordingBackend`; enumerated in the PR body. Touches: `_client_composition.py:280-305`, ARCH (`docs/architecture.md:476` currently promises the manual `ArtifactsAPI(rpc=...)` constructor — update). Acceptance: `_artifacts.py` imports nothing from `_web`; composition root is the only **production** constructor call site.

**R1.2 Dead code.** Delete `project_source_add_error` (`_backend_compat.py:898-918`, zero callers) and the already-dead `transport_factory` constructor parameter (`backend.py:116,148`: `del transport_factory`; signature pins in `test_binding_core.py`, `test_semantic_p8_provider_characterization.py`, `test_semantic_p8_provider_runtime_regressions.py` and the 13 files constructing `WebRpcBackend(...)` directly are updated) → 15 → 14 parameters. `self._reqid` (`backend.py:143`, write-only today) is **kept** — R2.2 passes it to `WebTransport`; the P8 inventory pins `_reqid_counter` (`test_semantic_p8_provider_boundary_audit.py:104`). (Research projection is **not** a quick win — moved to R6.4.)

**R1.3 Positional payload builders under `_web/codec` (I2).** Move the pre-move _artifact/payloads.py (imports `rpc` enums and `nest_source_ids`, `:8-23`; its production importers are the three codecs and `_artifact/__init__.py:8` (`from . import … payloads`), plus the 13 test/script files whose path references form the payload/markdown union (including `scripts/check_rpc_health.py:87` and `_web/codec/sources.py:568`), enumerated in the PR body; `_idempotency_policy.py:112,144` and `_row_adapters/artifacts.py:75,515` mention it in comments only, so no `_row_adapters → _web` cycle appears) to `_web/codec/artifact_payloads.py (new)`. **Split** `_artifact/formatters.py`: the three data-table helpers `_extract_cell_text` (`:130-143`, a dependency of `_parse_data_table`), `_extract_data_table_rows` and `_parse_data_table` (`:146-213`, `RPCMethod.LIST_ARTIFACTS`, `safe_index`) move to `_web/codec/artifact_formatters.py (new)`; the pure HTML/markdown helpers `_extract_app_data`/`_format_interactive_content` (`:31-130`) stay in `_artifact/formatters.py` with its `rpc` imports stripped, because they are imported above the port by `_artifacts.py:18` and `_studio/representations.py:8` (moving them whole would make a facade and an I1 service import `_web`). Relocate the pre-move _source/markdown.py (pure text conversion) to `_markdown.py (new)`; keep `_research_neutral.py` as a permitted neutral helper. Touches: WPI, RBI, ARCH. Acceptance: I2 allowlist loses the six payload/formatter/markdown entries (10 → 4); `_artifacts.py` and `_studio/representations.py` import nothing from `_web`.

### R2 — Chat (≈10 d, 5 PRs in order)

**R2.1 Codec owns the wire and emits records (S3-a/b; C1, C2; D2).** Merged from rev-1 R2.1+R2.2 (both codec-only; rule 1). Move `_chat/stream_request.py` into `_web/codec/chat_stream.py` (`encode_ask_stream`); `parse_citations`/`parse_single_citation`/`attach_answer_anchors`/`_ChunkExtraction`/`StreamingChatParseResult` operate on `ChatReferenceRecord`; `ConversationTurnKey` (built in `_row_adapters/chat.py:870-905`) and `NextStepSuggestion` (`chat_stream.py:460-464`) construction is replaced by the existing `ChatTurnKeyRecord`/`ChatNextStepRecord` (`_chat_records.py:51`), projected at the facade by the existing `_projectors.py:470`; delete `_chat/stream_decode.py`, `_chat/wire.py` (its one production helper `project_legacy_conversation_history`, `wire.py:35-40`, used by `api.py:64,768`, moves to `_chat/history.py`; `history.py:13`'s import of `_extract_next_turn_content` is replaced by the codec-produced record field), `_chat/transport.py`, `_chat/notes.py` (`_resolve_reference`/`build_save_chat_as_note_params` have no production user), the `_chat/__init__.py` test-only re-export (`:16-25`), and `ChatAPI._build_chat_request`, `_parse_ask_response_with_references`, `_extract_answer_and_refs_from_chunk`, `_raise_if_rate_limited`, `_parse_citations`, `_parse_single_citation`, `_extract_text_passages`, `_extract_uuid_from_nested` (`api.py:56-66,772-830`). Tests: 8 files import the shims, 8 more reference `ChatAPI._parse_*` (incl. `tests/_guardrails/test_cassette_shapes.py`, `tests/e2e/conftest.py`); `test_semantic_compatibility_regressions.py:100-136` tests the shim's own thread-safety and is **deleted** with it; `test_streaming_chat_wire.py:338` re-points to `notebooklm._web.codec.chat_stream.strip_anti_xssi` (the codec already resolves the module global at call time, `chat_stream.py:42,170` — no import-style change). Add a `_chat/` ban on `rpc.encoder`/`nest_source_ids` imports (the `tests/_guardrails/_wire_contract.py` index guard already scans `_chat/` but `stream_request.py:44-54` is list construction, not index reads). Touches: CVI (`chat_stream.py` loses its `ChatReference` allowance), WPI, `test_p3_codec_model_boundary.py:38-45`, ARCH. Acceptance: I2 loses `codec/chat_stream.py` and `codec/chat.py` (4 → 2); decoded goldens byte-identical.

**R2.2 Stream as a native kind; `CHAT_ASK` service-owned (S3-c; C3, N4; D9).** Resolves the rev-1 contradiction (`registry.py:139,333` forbid a row on a service-owned op): add one `PRIMITIVE` leaf **`CHAT_STREAM_ANSWER`** (input: the stream data; output: a new `ChatStreamResult(answer: ChatStreamAnswerRecord, raw_response: str)` in `_chat_records.py` — the existing `ChatStreamAnswerRecord` `:227-235` carries no `raw_response`, yet `ChatAskResultRecord.raw_response: str` is required (`:244`) and the row stores the first 1,000 characters (`bindings/chat.py:184`; `test_web_binding_rows_chat_ask.py` pins the result shape)) as a codec row, and make product `CHAT_ASK` service-owned: `ChatService.ask` = invoke(`CHAT_STREAM_ANSWER`) → resolve the conversation id from `value.resolved_conversation_id` exactly as the row does today (`chat.py:184-186`; the stream does not return one) → if `None`, invoke(`CHAT_GET_CONVERSATION`), calling `require_leaves` for both leaves before streaming (principle 5), and on a readback failure raising `rebind_operation(mark_backend_outcome_unknown(error), Operation.CHAT_ASK)` from the original cause (`_backend.py:243-282`) so the failure is attributed to `chat.ask`, not to the `CHAT_GET_CONVERSATION` leaf, as the P9.2 workflows do (`invoke` has no `outcome_unknown_on_expiry` option, `_backend.py:97-105`); the `ChatError("Server did not register a conversation…")` raise (`bindings/chat.py:213-217`) becomes `BackendError(reason=CHAT)` with the message in diagnostics, reconstructed byte-identically by `_backend_compat`.
Mechanism (corrected): `NativeChoice` becomes `RpcNative(method, variant) | StreamNative(label)` and `invoke_binding` dispatches `StreamNative` rows through `Transport.stream` (today only `RowInvoker.stream` can); the codec returns a `StreamRequestPayload` **data** value; the synchronous `Transport.assemble_stream` (`_binding.py:200`) only packages it into a `WebStreamRequest`; the **async** `WebTransport.stream` then awaits `next_reqid()` on the shared `ReqidCounter` passed in as the existing `reqid` parameter (`next_reqid` is a coroutine, `_reqid_counter.py:172`; this moves its single call site, `bindings/chat.py:138`; `chat_reqid` and `reqid` are already one object), builds the `BuildRequest` closure from the data (`RuntimeTransport.perform_authed_post` must still receive a closure — it calls `state.build_request(snapshot)` and rebuilds after refresh, `_runtime/transport.py:119-181`), holds the `set_request_id` token across the whole awaited POST and resets it in `finally` (`chat.py:151-171`), and computes `read_timeout = min(chat_timeout, deadline.remaining())`, preserving `_format_chat_read_timeout_message` (`_web/chat_transport.py:34`). `chat_transport`, `chat_timeout`, `chat_response_max_bytes`, `reqid` remain `WebRpcBackend.__init__` inputs (the transport is built inside it, `backend.py:156-160`); `chat_reqid` is removed → **14 → 13** parameters (after R1.2's `transport_factory` removal). Delete `StreamSpec`, `StreamPayload`, `RowInvoker.stream`, `CustomBinding.streams` (`_binding.py:163-190,254-265`) — no users remain. Touches (deltas; absolute values depend on merge-queue order): OPS +1, SUP ±0 (row swap), SO +1, CRC protocol −1, KASO, WPI, RBI, CAT (specs: `CHAT_ASK` disposition + divergence), OBS (`chat_stream_success` scenario), I4 (three `chat_*` names go), and the constructor-signature/`vars(backend)` pins in `test_binding_core.py`, `test_semantic_p8_provider_characterization.py`, `test_semantic_p8_provider_runtime_regressions.py`. Acceptance: no chat collaborators; `test_chat_characterization`, `test_web_binding_rows_chat_ask` (rewritten for the leaf) green; error identities unchanged.

**R2.3 `ChatWorkflowService` (S5; C4).** New stateful service (`_chat/workflow.py (new)`) owning the two lock maps, `ConversationCache`, `DeletedConversationTracker`, the created-session hint, follow-up/turn-number logic, and the `reset_after_open`/`_on_loop_rebind`/`set_bound_loop` hooks; `ChatService` folds into it. `ChatAPI` keeps loop-guard assertion, `project_backend_error`, `AskResult`/`ChatSession` projection, and the cache inspection accessors delegating to the workflow. The three signatures that take `ChatAPI` — `ClientLifecycle.open(chat=)` (`_runtime/lifecycle.py:262,326,352`), `WebCookieProvider.open(*, uploader, chat)` (`_runtime/web_cookie_provider.py:198,219-220`), `WebRpcBackend.open_client(chat=)` (`_web/backend.py:205`) — are retyped to a `ChatLifecycleHooks` protocol the workflow service satisfies; `test_runtime_lifecycle.py:155-160` and the P8 inventory (`test_semantic_p8_provider_boundary_audit.py`) are updated. Touches: WPI, RBI, P8 audit inventory, `_client_composition.py`, ARCH. Gates: `tests/unit/concurrency/test_loop_affinity_guard.py`, `tests/integration/concurrency/test_cross_loop_affinity.py`, `test_runtime_lifecycle.py`, `test_chat.py`. Acceptance: `_chat/api.py` ≤ 350 lines (~450 lines move, `:93-410,513-590`); chat workflow tests run against `RecordingBackend`.

**R2.4 I2 for chat.** `backend.py:64,69` (`ChatAPI` TYPE_CHECKING import) goes with R2.3; I2 allowlist 2 → 1 (`backend.py` ↔ `mind_maps`/`sources` until R3/R4). (Folded into R2.3's PR; listed for traceability.)

**R2.5 Derive the tables (S8; I7; D3).** Sequenced **after R2.2** so the derivation is written once against the `RpcNative | StreamNative` union. `_web/registry.py`: `_SUPPORTED_DEFINITIONS` derived from `WEB_BINDING_ROWS`; service-owned defs/reasons stay explicit; the construction-time audit call (`registry.py:113,347-350`) is removed. `_web/policy.py` is **deleted**: its hand-reviewed expected natives, roles, expected policies, workflow leaf edges and `known_divergence` entries move verbatim to `scripts/_web_policy_intent.py` (beside `scripts/_operation_catalog_specs.py`; the reviewed intent stays independent of rows and registry; `scripts/` never imports `tests/`), the audit functions move to `scripts/audit_operation_catalog.py`, and the comparison result is stored in `web_policy_parity.json`. The `chat.ask` `GET_NOTEBOOK` entry is **kept** as the recorded end-to-end recency divergence (gate table `:576`); the audit reports direct-row parity and end-to-end authority as two columns. `scripts/_operation_catalog_ast.py:844-867`, `_operation_catalog_specs.py:615-629,1109`, `audit_operation_catalog.py:186-203,434` are re-pointed at the intent module. Touches: CAT (`active_web_policy_bindings`, `known_divergences`), baseline registry, `docs/development.md` table, ARCH. Importers to update — 21 touched files (19 direct `_web.policy` importers plus two guardrail references), enumerated in the PR body: 17 test files (`tests/unit/test_{label,collection,notebook,sharing,source,artifact}_*_workflow.py`, `test_web_binding_rows_{chat,chat_ask,primitives}.py`, `test_operation_catalog_rows.py`, `test_semantic_p4_convergence_characterization.py`, `tests/_guardrails/test_semantic_p8_provider_boundary_audit.py`), `test_no_rpc_method_below_port.py:86` (sanctioned list loses `policy.py`), `scripts/measure_web_backend_chain.py`. Sequenced **before** the row-table PRs (R3.2–R3.5, R4.2, R5.1) so none of them hand-edits the 1,120-line ledger. Acceptance: `_web/policy.py` deleted; `_web/registry.py` names no `RPCMethod`; parity audit green as a baseline test; the divergence count text (`docs/architecture.md:140`, ADR-0035 `:160`) unchanged.

### R3 — Source-add (≈8 d, 5 PRs; D5 addendum required)

**R3.1 Neutral failure graph first (S1-b; I8).** Evidence before hoist (plan `:2078-2081`): define the neutral upload port — a record-based callback/result/error contract for the Scotty pipeline (`_source/upload.py:432-529` today raises a broad post-registration graph that `translate_web_error`'s four reviewed families, `_web/errors.py:22`, do not cover) — and extend `translate_web_error` with the upload/registration reasons; every raw catch in `_source/add.py:84,273-276,327-360` and `_source/batch.py:157-185` (grpc-status branching, `exc.args` rewrite) becomes a neutral-reason branch with `rpc_code`/`source_id`/`stage` in diagnostics; confirm `SourceAddFailureRecord` carries `SourceAddError.source_id`/`stage` and the causal chain; delete `ErrorMode.RAW_PASSTHROUGH` from the enum and the four rows; `_sources.py` projects via `_backend_compat` (the shielded upload finalize at `_sources.py:1166` is not duplicated). Object-identity pins `test_web_binding_rows_sources_custom.py:437,500` (`caught.value is error`) are rewritten to an equality oracle over `type`, `method_id`, `rpc_code`, `raw_response`, `source_id`, `stage`, `unconfirmed`, `args`/`str()`, URL and retry/status/timeout fields, any `SourceAddFailureRecord` subclass fields (`_source_records.py:180`), `__cause__`, `__context__`, `__suppress_context__` (all restored by `failure_projection.py:205` / `_backend_compat.py:886`) — reconstruction yields equal, not identical, objects (`backend.py:395-399`). The neutral port translates only at the row boundary; the Scotty pipeline's internal legs keep their raw `httpx` semantics (`tests/unit/test_sources_upload.py:864,1028` pin `HTTPStatusError` from `_start_resumable_upload`). Retype `WebRpcBackend.open_client(uploader=)` / `_configure_default_upload_backend` to a narrow `UploadLifecycleHooks` protocol (as R2.3 does for chat) so `backend.py:69`'s `_source.upload` import goes and I2 can reach 0 under D4. Touches: I8 pin, WPI (no CRC change: R3.1 only strips `error_mode`; the `source.add_text` compatibility row, `sources.py:764-773`, is deleted in R3.2). Acceptance: upload-failure parity tests green with identical public exception **types and fields**.

**R3.2–R3.5 Hoist one operation per PR (S1-a; D5): `add_text`, `add_url`, `add_drive`, `add_url_batch`.** Add `SOURCE_REGISTER` (tier `PRIMITIVE`, +1 OPS) in R3.2; each PR ports its probe/baseline/reconcile/title-finalise workflow from `_source/add.py` (814 lines), `_source/batch.py` (383), `_source/drive_import.py` (717, drive only) onto `SourceRecord`s in `SourceService` using leaves `SOURCE_LIST` (the full snapshot reconciliation needs; `SOURCE_GET` takes one id, `_source_records.py:37,54`) / `NOTEBOOK_GET`, `SOURCE_REGISTER` (typed: registration kind, payload, idempotency variant), `SOURCE_PATCH_TITLE`; deletes its row and the `project_source`/public `Source` imports as the last one leaves `bindings/sources.py:50,95,170-238`. `capture_public_failure` leaves `ROW_COLLABORATOR_NAMES` with the last hoist. Touches (each): SUP −1, SO +1, CRC (R3.2: compatibility −1 for `source.add_text`; R3.3–R3.5: protocol −1 each), KASO, RBI, CAT; R3.2 also OPS +1 (`SOURCE_REGISTER`). Duplication window: `_source/add.py` helpers are shared by the url/text/drive rows while `SourceService` carries the record-based port — acceptable under rule 2 (one authority *per operation*) and closes at R3.4. Acceptance: I2 loses `bindings/sources.py`; `SourceService` returns records only; exact-id-diff attribution and idempotent-create characterization green.

### R4 — Notes and mind-maps (≈7 d, 2 PRs)

**R4.1 Split the legacy class out of `_note_service.py` (S2-a).** `NoteService` (`_note_service.py:475-684`) uses none of the module-level wire imports (`NoteRow` `:48`, `safe_index` `:50`, `RPCMethod` `:51` serve only `LegacyNoteBackedService`/`NoteRowKind`, `:78-473`). Move `LegacyNoteBackedService` + `NoteRowKind` into their co-consumer `_mind_map.py` (already imports both, `_mind_map.py:23,56`), which gets the `Removal: P10` docstring token; strip the wire imports from `_note_service.py`; re-point the production importers `_artifacts.py:29`, `_client_composition.py:28`, `_web/bindings/mind_maps.py:31` **and** every test/fixture/script importer of the moved types (`tests/unit/test_note_service.py:23-24`, `tests/_fixtures/note_stack.py:6`, `scripts/_operation_catalog_ast.py:943-1010`, and the rest of the files matching `LegacyNoteBackedService|NoteRowKind` under `tests/` and `scripts/`, enumerated in the PR body). No new module. Acceptance: importing `_note_service` loads no `rpc`/`_row_adapters`; CI green with no shim.

**R4.2 Mind-map workflows above the port (S2-b).** Correct row inventory: the `compatibility` rows are `artifact.generate_mind_map`, `artifact.list`, `artifact.get` (`bindings/mind_maps.py:329-365`) (+ `source.add_text`, R3.1); `mind_map.generate_note`/`generate_interactive` are `deferred-product` (`:302-327`, R5). `GENERATE_MIND_MAP` has **no leaf row** (it lives only inside `MIND_MAP_GENERATE_NOTE`, `:332-338`), so this PR adds **`MIND_MAP_GENERATE`** (tier `PRIMITIVE`, +1 OPS) and R5 reuses it. Persistence (`MIND_MAP_GENERATE` → `NOTE_CREATE` → `NOTE_UPDATE`, `NOTE_DELETE` on cancel) moves into `MindMapFamilyService` with the `asyncio.shield` structure ported verbatim from `_note_service.py:296-393,511-543`; catalog merging (`ARTIFACT_CATALOG` + `MIND_MAP_LIST` — `NOTE_LIST` filters mind maps out, `_web/codec/notes.py:102`, `bindings/mind_maps.py:65`) into `StudioCatalog`, preserving ADR-0019's exact partial-availability swallow set. ADR-0019 partial availability: `_catalog_records` swallows raw `httpx.HTTPError` (`mind_maps.py:224-233`) because the auth-refresh-failure path deliberately re-raises a raw `HTTPStatusError` (`_runtime/transport.py:227-233`; pinned at `tests/unit/test_client.py:1062-1065`) and `invoke` translates only `NotebookLMError` (`backend.py:412-435`). The net is therefore **scoped to the supplemental branch only**: a row-level `map_error` on the `MIND_MAP_LIST` leaf translates `httpx` errors into `BackendError(reason=NETWORK)` with the original attached, and the service swallows by reason for that leaf; the primary `ARTIFACT_CATALOG` failure is **not** mapped — its raw `HTTPStatusError` is re-raised unchanged, because `_backend_compat.py:345-352` would project a `BackendError(NETWORK)` as `NetworkError` and `_artifacts.py:271-278` callers observe the raw type today (`_web/runtime.py:325-326`). Characterizations cover `artifacts.list`/`get`/`rename` and `notebooks.list()` on the refresh-failure path (rule 3: no public exception-type change). `MIND_MAP_GENERATE` has one output, `MindMapGenerateNoteResult.tree_json` (`_records.py:336-339`); the `(json, data, title)` derivation (`codec/mind_maps.py:251+`) moves into `MindMapFamilyService`. Delete `_web/bindings/_invoker_caller.py`, `_NOTE_FAMILY_SPECS`, `LegacyNoteBackedService` (now in `_mind_map.py`), the parallel legacy graph in `_client_composition.py:280-305`, and `ArtifactsAPI`'s `mind_maps` input (`NotebookLegacyRpc` is deleted in R6.2, where its consumer migrates); migrate `NotesAPI.list_mind_maps/delete_mind_map` (`_notes.py:241-275`, today on a raw `RpcCaller`) to `NoteService`. Touches: OPS +1, SUP, SO, CRC (the three mind-map compatibility rows go; the category reaches 0 once R3.2 has removed `source.add_text` — edge R3.2 → R4.2 for that statement), KASO, WPI, RBI, CAT, ARCH. Gates: `test_note_service.py:278,349`, `test_semantic_notes_mind_maps_slice_characterization.py:461`, `test_mind_map_*`. Acceptance: `LegacyNoteBackedService` and `_web/bindings/_invoker_caller.py` deleted; no mind-map compatibility rows.

### R5 — Studio generation (≈6 d, 2 PRs; needs D1, R0.2, R4.2 for the mind-map leaf)

**R5.1 Resolve inputs above the port (S4).** `NotebookReadService.get_source_ids` (`_read_services.py:57-68`) gains a diagnostics mode carried as a field on `NotebookGetInput` and applied by the codec at decode time (`_web/codec/source_ids.py:25` consumes raw wire, so the mode travels through the neutral input); reuse the existing `SourceIdDiagnostics` enum (`source_ids.py:23-25`) by relocating it to a neutral records module so the input record does not import `_web`; language defaulting and option validation move from `_web/codec/generation.py:117-211` to `_studio/*` (same public error types via the facade). Scope is **11 rows**: 8 `artifact.generate_*` (`studio.py:402-487`), `mind_map.generate_note`, `mind_map.generate_interactive`, `notebook.suggest_prompts` (`GET_NOTEBOOK` + `SUGGEST_PROMPTS`; the read moves above the port, `SUGGEST_PROMPTS` becomes the single native). Family-specific validation/read ordering is pinned before the move (audio validates before source resolution, `studio.py:191`; report resolves first, `:351`) together with the warn-logger output. Row shape per D1 (Codex recommends per-family typed resolved leaves — option (b)). Land as three PRs for revert granularity, as R3 does: **R5.1a** the 8 `artifact.generate_*`, **R5.1b** the 2 `mind_map.generate_*`, **R5.1c** `notebook.suggest_prompts`. Touches (per PR): CRC deferred-product −8 (R5.1a), −2 (R5.1b), −1 (R5.1c) → 0 and the category deleted in R5.1c; SUP/SO per D1; KASO, CVI, WPI, RBI, CAT; VCR cassettes `tests/integration/test_artifact_generation_idempotency.py`, `test_artifacts_integration.py` must replay unchanged (`freq` match). Serialize with R6.1 (both edit `_read_services.py`).

**R5.2 `_studio` I1 (N1).** `_studio/catalog.py`, `mind_maps.py`, `representations.py`, `lifecycle.py` stop importing projectors/public types; projection moves to `ArtifactsAPI`/`MindMapsAPI`. `downloads.py` stays as the named exemption. Acceptance: I1 allowlist −4.

### R6 — Return-type invariant, one domain per PR (≈8 d)

Can start after R0.1 (no dependency on R2–R4 except where noted). **R6.1** `_read_services` (Notebook/Source; serialize with R5.1). **R6.2** `_notebook_mutation_service` + `_notebook_guide_service` + `_suggestion_service`; also migrates **`NotebooksAPI.get_raw`** (`_notebooks.py:488-505`; line 501 is its raw `rpc_call(RPCMethod.GET_NOTEBOOK, …)`, the only consumer of `NotebookLegacyRpc`, `:37,76`) through `NOTEBOOK_GET` with a `NotebookGetInput` flag whose result carries the raw payload (`NotebookGetResult`, `_notebook_records.py:90-95`, cannot today; `invoke` enforces `type(result) is output_type`, `backend.py:436`) — **no SO delta**, public return `Any` preserved, encoded request byte-identical (cassette `freq` match). `get_metadata` (`_notebooks.py:523-550`) is a separate two-read composition: if `notebook.metadata` is to become service-owned it gets its own typed `NOTEBOOK_METADATA_DEF` and workflow (SO +1, CAT; `test_operation_catalog.py:681-704`); otherwise it stays `UNSUPPORTED` with the reason. `NotebookLegacyRpc` is deleted here (I9 target). **R6.3** `_sharing_service` + `_settings_service`. **R6.4 Research (S7, formerly "quick win").** `ResearchService` propagates `BackendError`; its four public-type branches become reason/diagnostics checks: `:571-580` (`except (RPCTimeoutError, RPCError)` + `_is_import_research_failed_precondition` on `rpc_code` → `reason`/`diagnostics["rpc_code"]`, `_web/errors.py:31-36`, define the FAILED_PRECONDITION neutral contract), `:467` and `:648` (`except (NetworkError, RPCError)`), and `_source_lister.list` (`:465`, a facade call — replace with the read service). `ResearchAPI` (`_research.py`, today imports no projector) gains `project_backend_error` across `start/poll/wait_for_completion/cancel/import_*`; add a `__cause__` characterization for the retried path (`test_research.py:730,754`, `test_semantic_research_slice_characterization.py:400`). N1 here is more than moving projection: the wait and verification workflows consume public research/source types throughout (`_research_service.py:102,380,423`), so this PR defines neutral workflow inputs/results and a record-based source lister, moves public validation and task construction to `ResearchAPI`, adds typed `OperationDef`s for `research.wait`/`research.import_verify` and flips their disposition to `SERVICE_OWNED` (SO +2, CAT). Budget 3 d. **R6.6 Notes and mind-map records.** `NoteService` returns public models today (`list_notes -> list[Note]` `:483`, `get_note_or_none` `:493`, `create_note -> Note` `:503-514`, `list_mind_maps -> list[MindMap]` `:588`, `get_mind_map_or_none` `:605`, via `project_note`/`project_mind_map` `:30`); it switches to `NoteRecord`/`MindMapRecord`, with projection in `NotesAPI` (`_notes.py:22-28`, already wraps `project_backend_error`) and `MindMapsAPI`. After R4.2. **R6.5** final purge: gated on R2.3, R3.5, R4.2, R5.2, R6.1–R6.4, R6.6 — I1 allowlist reduced to the `downloads.py` exemption; guardrail switches to hard rule.

### R7 — Structure (≈6 d, 3 PRs; no shims)

**R7.1–R7.3** Move, in one PR per family and updating every importer in the same PR (rule 1; `test_no_anonymous_bridges`): (1) records + projectors + compat → `_semantic/{records/,projectors.py,compat.py}`; (2) services → `_semantic/services/`; (3) port (`_operations`, `_backend`, `_binding`) → `_semantic/`. Measured importers: **130 files** by dotted path (110 `tests/unit`, 5 `tests/_guardrails`, 4 `scripts`, 3 `tests/integration`, 1 baseline JSON) plus `test_module_size_ratchet.py` allowlist keys, `docs/architecture.md` (54 refs, `check_docs_module_refs.py`), the catalog walker's hard-coded module/import triples (`scripts/_operation_catalog_ast.py:1048,1162,1489`), and the 11,265-line CAT regen (path-only diff). Budget 6 d. Acceptance per PR: `ruff`/`mypy`/CI green; CAT diff path-only.

---

## 4. Sequencing and dependencies

```
R0.0 addenda ──► R0.1 ──► everything
R0.1 ──► R0.2 ──► R2.2 (CHAT_STREAM_ANSWER is PRIMITIVE), R3.2 (SOURCE_REGISTER), R4.2 (MIND_MAP_GENERATE), R5, R6.2/R6.4 (disposition flips rely on workflows/available())
R1.2 ──► R2.2 (both edit the constructor signature/vars pins; 15 → 14 → 13)
R0.3 independent (after R0.1) ──► R2.2 (the unified check exists before the first StreamNative row)
R1.1, R1.2, R1.3 independent (after R0.1); R1.3 ──► I2 accounting for R2.1
R2.1 ──► R2.2 ──► R2.3 ; R2.2 ──► R2.5 ──► R3.2, R4.2, R5.1 (no PR hand-edits the old ledger)
R3.1 ──► R3.2 ──► R3.3 ──► R3.4 ──► R3.5 (each one authority; any prefix is a valid stopping point)
R4.1 ──► R4.2 ;  R4.2 needs R0.2 ; R3.2 ──► R4.2 (for the "compatibility rows = 0" statement only) ; R4.2 ──► R6.6
R5.1 needs D1, R0.2, R4.2 (MIND_MAP_GENERATE leaf); R5.1 ∥ R6.1 serialized (shared _read_services.py)
R6.1–R6.4 after R0.1, independent of each other; R6.6 after R4.2; R6.5 after R2.3, R3.5, R4.2, R5.2, R6.1–R6.4, R6.6
R7 last
```

**Merge discipline.** The PR queue is PR1 → PR2 → {PR3 → PR4 → PR5} → PR6 → PR7; CAT and the count pins are regenerated once per PR merge (the per-slice deltas below are cumulative within a PR and are what the PR body reports; the slice order inside a PR is R0.2 → R2.2 → R2.5 → R3.2 … as listed). Per-PR deltas along that order (OPS / SUP / SO): R0.2 0/0/0 (96/80/11); R2.2 +1/0/+1 (97/80/12); R2.5 0/0/0; R3.2 +1/0/+1 (`SOURCE_REGISTER` row added, `add_text` row removed → SUP net 0; 98/80/13); R3.3–R3.5 0/−1/+1 each (98/77/16); R4.2 +1/−2/+3 (`MIND_MAP_GENERATE` row added, `artifact.list`/`artifact.get`/`artifact.generate_mind_map` rows become service-owned: 77 + 1 − 3 = 75 → 99/75/19 under D1(a)); R5.1a–c under D1(a) 0/0/0 (rows stay direct as codec rows) or under D1(b) +10/… (`MIND_MAP_GENERATE` is reused, so ten leaves, max OPS 109); R6.2 0/0/+1 only if `NOTEBOOK_METADATA_DEF` is added (`get_raw` migration itself is 0/0/0); R6.4 0/0/+2 (`research.wait`, `research.import_verify`). Non-row PRs (R2.1, R2.3, R3.1, R4.1, R6.x, R7.x) parallelize safely. R1.1, R2.3, R4.2 all touch `_client_composition.py:260-305` — rebase order R1.1 → R2.3 → R4.2. R2.3 and R3.1 both retype `WebRpcBackend.open_client` (`backend.py:205`; `chat=` → `ChatLifecycleHooks`, `uploader=` → `UploadLifecycleHooks`) plus `web_cookie_provider.py` and `lifecycle.py` — rebase order R2.3 → R3.1; the second re-lists the P8 inventory diff.

**Estimate:** ≈59 engineer-days across 7 PRs (31 slices; see §3a) (rev 1 said 36–40 across 19; deltas: R2 +4, R3 +1 and its split, R6 +4 (research neutral contracts), R7 +4, R5.2 +1). Re-estimate after R0.0's addenda fix the leaf contracts.

---

## 5. Risks and mitigations

| Risk | Mitigation and real gate |
|---|---|
| Public exception identity/field drift when `RAW_PASSTHROUGH` goes (R3.1) or projection moves (R6) | Per-domain characterization test asserting `type`, `method_id`, `rpc_code`, `raw_response`, `__cause__` against a recorded failure before/after; identity (`is`) pins rewritten and listed in the PR body |
| Cancellation-safety in mind-map finalisation (R4.2) | Port `asyncio.shield` verbatim; gates `test_note_service.py:278,349`, `test_semantic_notes_mind_maps_slice_characterization.py:461` |
| Loop-affinity regressions (R2.3) | Keep hook names/order; gates `tests/unit/concurrency/test_loop_affinity_guard.py`, `tests/integration/concurrency/test_cross_loop_affinity.py`, `test_runtime_lifecycle.py:155-160` |
| Wire drift in R5 | `freq`-matched cassettes replay unchanged |
| Baseline churn (ADR-0022) | One regeneration per PR, reviewed as a diff; never regenerate to accept drift; counts listed per PR |
| Test-count shrinkage hiding lost coverage | `pytest --co` count per PR; deletions enumerated |
| ADR-0019 partial availability lost when `httpx` leaves services (R4.2) | Row-level `map_error` on the supplemental `MIND_MAP_LIST` leaf only translates `httpx` errors to `BackendError(reason=NETWORK)` (never a blanket transport translation — that would change a public exception type on the auth-refresh-failure path); the existing partial-catalog tests plus a refresh-failure characterization are the gate |

---

## 6. Definition of done — measurable

"Now" is measured from the live tree at R6.5 (2026-08-26), after PR1–PR6 and
the I2 close-out; the pre-programme figures it replaces are in the revision
history. Eleven of the fourteen rows are at target; the three that are not are
marked **short** and say what is left.

| Metric | Now | Target |
|---|---|---|
| Custom rows (protocol / compatibility / deferred-product) | 1 / 0 / 1 — **short**: `mind_map.generate_note` stays deferred-product (input-defaulting; hoisting it needs a resolved-input primitive for the note) | 1 (upload) / 0 / 0 |
| `ROW_COLLABORATOR_NAMES` | 1 — exactly `{"source_uploader"}` | exactly `{"source_uploader"}` |
| I1 allowlist | 1 — the named exemption only; R6.5 deleted the drained seed and I1 is now a hard rule | 1 (`_studio/downloads.py`, named exemption) |
| I2 allowlist | 0 — `bindings/sources.py` drained last and I2 is now a hard rule with no exemption. Its `_source.add` edge was `honor_requested_title`, reached by the permanent `SOURCE_ADD_FILE` row under D4, so there was nothing above the port to hoist it to; it is not neutral either (public `Source`, raw `RPCError`/`NetworkError`), so a neutral module would have recorded the coupling as compliance. It moved *down* into `_web/bindings/sources.py` beside the only row that calls it, and the 68-line `_source/add.py` was deleted | 0 (`backend.py:69` retired by R3.1's `UploadLifecycleHooks`) |
| `RAW_PASSTHROUGH` rows / enum member | 0 / deleted (`ErrorMode` is `{TRANSLATE, TRANSLATE_SCRUBBED}`) | 0 / deleted |
| `RPCMethod` tokens in `_web/policy.py` | 0 — module deleted; the reviewed intent moved as planned | 0 (module deleted; reviewed intent lives in `scripts/_web_policy_intent.py`) |
| `_web/registry.py` hand-written direct defs | 0 — the supported set is derived from the row table | 0 |
| `WebRpcBackend.__init__` parameters | 13 | 13 (`transport_factory` dead — R1.2; `chat_reqid` duplicate — R2.2; `reqid` feeds the transport) |
| `_chat/api.py` lines | 517 — **short** of the ceiling, though down from 830 | ≤ 350 |
| I9 deletion targets (`LegacyNoteBackedService` in R4.2, `NotebookLegacyRpc` in R6.2) | 0 — both deleted; R6.5 removed the target set itself and restated I9 as a hard rule over its enumerated exemptions | 0 |
| `capabilities.available()` true for service-owned ops (incl. research wait/import_verify after R6.4) | yes | yes |
| `Operation` members / product members | 99 / 87 (12 primitives) | 99 under D1(a), 109 under D1(b) / 87 |
| Root-level `_*_service.py` / `_*_records.py` modules | 10 / 10 — **short**: this is R7's structure work, which has not landed | 0 |
| Public API, cassettes, adapter payload baselines | unchanged — no cassette and no public-surface baseline moved; `json_envelope.json` moved only in its private-model provenance (`evidence`, `shape_derivation`, `private_dataclass_projection_paths`), with every emitted key set and projection id identical | unchanged |

---

## 7. Review protocol

Reviewers return `VERDICT: APPROVE|REVISE`, numbered BLOCKING (factual error, missing dependency, compatibility break, unmeetable acceptance criterion), numbered NON-BLOCKING, each with file:line. Convergence = all `APPROVE` or only non-blocking items remain; hard cap three rounds.

---

## 8. Open items for the owner

- D1 is an owner decision; the plan is written for (a) and lists the deltas for (b).
- `_studio/downloads.py`'s permanent I1 exemption should get its own later slice (download transport under `_web`); not in P10.

## 9. Change log (rev 1 → rev 2)

Governance: added D5–D8 and the R0.0 addenda PR; `supports()` semantics kept (D6); I1 now backed by an ADR addendum (D7); D1 option (c) rejected. Inventory: I1 seed 8 → 13, I2 5 → 10 (with retiring PR per entry, new R1.3), I9 2 → 12 with exemptions; custom-row categories corrected (`source.add_text` compatibility; `mind_map.generate_*` deferred-product); `GENERATE_MIND_MAP` leaf added (R4.2); `SUGGEST_PROMPTS` scoped into R5. Chat: R2.1+R2.2 merged; `CHAT_ASK` contradiction resolved with the `CHAT_STREAM_ANSWER` primitive; transport mechanism corrected (closure built by `WebTransport`, reqid single call site, three chat params remain, 15 → 13); `StreamSpec` family deleted; R2.3 lists the three `ChatAPI` signatures and the P8 inventory; test counts corrected (8 + 8 files; thread-safety test deleted, not re-pointed). Source-add: evidence (R3.1) before hoist; hoist split per operation; identity pins rewritten; D5 addendum. Notes: R4.1 creates no new module; `NotesAPI` mind-map methods and the `mind_maps` ctor input moved to R4.2; `httpx` translation for ADR-0019. Research (R1.2) rescoped to R6.4 with its four catch sites. Tables (R1.3 → R2.5) after the stream native; stored-policy baseline (D3); catalog specs and docs text listed. R7: no shims, 130 importers, three PRs, 5 d. Per-PR pins & baselines listed; real gate tests named; `_records.py` size ceiling (I10). Estimate 36–40 → ≈55 d. Rev 3.1 (lens C on the packaging): pins/CAT in each slice commit (bisectability); stacked branches PR3→PR4→PR5; merge-gate column completed; linear revertibility stated; ≈59 d. Rev 3.0 (owner direction): repackaged into 7 workstream PRs (§3a); slices unchanged; estimate +2 d for rebase cost. Rev 2.8 (Codex delta check): payload/markdown importer union stated as 13 files total; `_web.policy` inventory 21 touched files. Rev 2.7 (round 3, Codex): all three data-table helpers move (`_extract_cell_text` dependency); `check_rpc_health.py:87`; readback failures rebound to `CHAT_ASK` via `rebind_operation` + `require_leaves`; R4.1 test/fixture/script importers enumerated; `httpx` net confined to the supplemental `MIND_MAP_LIST` branch with artifact list/get/rename characterizations; R6.2 corrected — `_notebooks.py:501` is `get_raw` (0/0/0), `notebook.metadata` needs its own def; 17 test importers. Rev 2.6 (round 3, lens A non-blocking): real `payloads` importers (`_artifact/__init__.py:8` + 13 test/script files); line-ref drift (`backend.py:116,148`, `_chat_records.py:244`); R5.1 CRC per-PR deltas. Rev 2.5 (round 3, lens C non-blocking): `payloads.py` consumer list corrected (comment mentions only); edges R1.2 → R2.2 and R0.2 → R6.2/R6.4; `open_client` rebase order R2.3 → R3.1; R6.2 raw-return mechanism; 31 PRs. Rev 2.4 (round 3, lens B + agy non-blocking): intent path aligned to `scripts/` everywhere; risk row 7 reworded to the row-level net; R4.2 SUP 75; 20 `_web.policy` importers; R6.2 byte-identical acceptance. Rev 2.3 (round 2, Codex): `CHAT_STREAM_ANSWER` outputs `ChatStreamResult(answer, raw_response)`; conversation id resolved from `value.resolved_conversation_id`; reqid allocation and the logging token move into the async `WebTransport.stream` (sync `assemble_stream` only packages data); dead `transport_factory` removed in R1.2 (15 → 14 → 13); one stated merge queue with per-PR OPS/SUP/SO deltas (R3.2 SUP net 0; D1(b) max 109); fuller oracle fields. Rev 2.2 (round 2, lenses A/B/C): `formatters.py` split (pure helpers stay above the port); R4.1 moves the legacy class into `_mind_map.py` instead of a new module; R2.5 acceptance rescoped to `policy.py`/`registry.py`, 16+2 importers listed, sequenced before the row-table PRs, intent in `scripts/`; R4.2 `httpx` net scoped to the catalog leaves with a refresh-failure characterization; `NotebookLegacyRpc` deletion moved to R6.2 with `notebook.metadata` routed through `NOTEBOOK_GET`; new R6.6 for `NoteService` records; `reqid` kept (15 → 14); `mark_backend_outcome_unknown` for the readback; `UploadLifecycleHooks` retires `backend.py:69`; per-PR deltas instead of absolute pins; signature/`vars` pins listed; R5.1 split a/b/c; `SourceIdDiagnostics` relocated; line-ref drift fixed; 30 PRs / ≈56 d. Rev 2.1 (Codex round 1): I1 permits neutral scalars; D2 reversed (existing chat records used, no exemption); D3 reworked — reviewed intent stays independent but moves test-side, `_web/policy.py` deleted; D9 chat addendum; R0.2 keeps research wait/import_verify unsupported until R6.4 adds typed defs; `chat.ask` `GET_NOTEBOOK` divergence kept as intentional; `StreamNative` dispatch in `invoke_binding`; R3 leaves `SOURCE_LIST`, neutral upload port, fuller exception oracle; R4.2 uses `MIND_MAP_LIST`; R5.1 diagnostics mode through `NotebookGetInput`, ordering pins; R6.4 neutral research contracts; R7 walker triples; `architecture.md:476`.
